package arrays

func MaxArea(heights []int) int {
	// validate len(heights) <= 0 return 0
	// heights cannot be negative

	/*
			st := 0
			en := len(heights)-1

		area = min(h[st], h[en]) * (en - st)
		if h[st] < h[en] {
			st++
		} else {
		en--
		}
	*/

	return 0
}
